"""
Data Operations for Car Insurance ML Pipeline (submit_directory version).

In this version, the ingest_data job runs as a standalone script via
MLJobDefinition.register(). See jobs/ingest_data.py for the job code.

This module retains any warehouse-side data helpers if needed.
"""
